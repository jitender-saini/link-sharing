package com.ttn.groovy

class EmailService {
    void sendCancellationEmail(User user, String content){
               //sends cancellation email
    }

    void sendPasswordResetEmail(User user, String content){
               //sends password reset email
    }
    void sendPasswordResetFailureEmail(User user, String content){
               //sends password reset email
    }
}
